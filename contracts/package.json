{
  "name": "chainwork-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "ChainWork phase-based escrow smart contracts (Hardhat, Polygon Amoy testnet).",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
